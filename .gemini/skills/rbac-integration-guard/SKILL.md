---
name: rbac-integration-guard
description: RBAC 权限集成与 SQL 自动化守卫。当新增功能、API 或页面时使用。强制执行权限自动注册、HQ 角色自动授权及 SQL 脚本自动执行/同步。
---

# RBAC Integration Guard

## 核心原则

在 Smart-CS Pro 中，任何功能（API、UI、动作）必须实现“开发即授权”。

### 1. Skill 自我进化红线 (强制)
*   **主动补全**：若在开发中发现 Skill 描述不准确、缺失必要的约束（如环境依赖、语法限制）或可以优化操作流程，**必须立即主动修改并重新打包该 Skill**，无需等待用户指令。
*   **知识固化**：每一项由于疏忽导致的错误（如 MySQL 兼容性问题）必须总结为一条“红线”写入对应的 Skill 中。

### 2. 权限自动同步红线 (强制)
*   **开发即注册**：每当新增页面、干预动作或敏感 API 时，必须立即生成 `INSERT INTO permissions` 语句。
*   **总部角色 (HQ) 特权自动维持**：在新增任何权限点后，**必须同步生成并执行**为 role_id=3 赋予该权限的 SQL。
*   **脚本固化**：所有变更必须同步追加到 `central_production.sql`。

### 2. SQL 自动化执行工作流
*   **热更新**：完成 SQL 编写后，优先尝试使用 `run_shell_command` 通过 `mysql` 命令行直接执行该 SQL 片段。
*   **重置策略**：如果架构变更过于复杂导致执行失败，在征得用户同意或为了保持环境一致性的情况下，可以执行 `DROP DATABASE` 并重新运行全量 `central_production.sql`。

## 操作指南

### 场景 A：新增功能点（以新增“实时录音协助”为例）
1.  **标识定义**：确定代码为 `command:voice:assist`。
2.  **生成 DDL/DML**：
    *   `INSERT INTO permissions (code, name, module) VALUES ('command:voice:assist', '实时录音协助', '实时指挥');`
    *   `INSERT INTO role_permissions (role_id, permission_code) VALUES (3, 'command:voice:assist');`
3.  **自动执行**：
    *   读取 `.env` 中的 DB 配置。
    *   调用 `run_shell_command` 执行 SQL。
4.  **同步主脚本**：将上述语句追加至 `central_production.sql`。

### 场景 B：API 权限校验
*   在 API 逻辑中，必须强制校验 `current_user["role_id"]` 或检查 `role_permissions` 关联。

## 示例 (自动化反馈)

> 🚀 **战术功能已同步：**
> 
> 我已为您新增了“XX功能”，并执行了以下自动化守卫动作：
> 1. 在 `permissions` 表中注册了权限点。
> 2. **自动为总部身份（HQ）赋予了该功能的全量访问权。**
> 3. 已通过命令行自动在 MySQL 中执行了同步 DDL。
> 4. 变更已固化至 `central_production.sql`。
