2. 检查端口占用情况（如果上述命令无效）
   如果你想查看具体是谁占用了端口：
   
   1 lsof -i :8000
   你会看到类似这样的输出：
   python3  68266  song   3u  IPv4 ... TCP *:8000 (LISTEN)
   
   然后使用输出中的 PID（例如上面的 68266）手动杀死它：
   1 kill -9 68266

3. 重新启动
   清理干净后，再次运行启动脚本或直接启动：
   
   1 # 方式 A：进入目录直接调试启动
   2 cd SmartCS_Desktop/smart-cs-pro/core_engine
   3 python engine.py
   4
   5 # 方式 B：使用守卫进程后台启动
   6 cd SmartCS_Desktop/smart-cs-pro/core_engine
   7 python guardian.py > ../../../engine.log 2>&1 &
   
   提示： 以后在修改代码后，只需执行 pkill -f "python
   engine.py"，守卫进程会自动帮你重启一个干净的新实例，而不会出现端口占用的冲突。~~~~







  为了让 Electron 能够自动找到并运行这个引擎，最佳存放位置是 Electron 的 `resources` 目录（或与其同级的开发目录）。

  推荐的部署路径结构：

   1 SmartCS_Desktop/smart-cs-pro/
   2 ├── resources/           # 这里存放 icon 等资源
   3 │   └── SmartCS_Engine.exe # 将打包好的文件放在这里
   4 ├── package.json
   5 └── 





chcp 65001 


