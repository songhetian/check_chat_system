# [Smart-CS Pro] 数智化战术指挥系统 - 环境配置文件

# ==========================================
# 1. 数据库配置 (Database Configuration)
# ==========================================
# 数据库类型选项: sqlite (本地模式), mysql (中心化模式)
DB_TYPE=mysql
# 数据库服务器地址
DB_HOST=192.168.2.184
# 数据库端口 (MySQL 默认为 3306)
DB_PORT=3306
# 数据库访问账号
DB_USER=tian
# 数据库访问密码
DB_PASSWORD=tian@123456
# 数据库名称
DB_NAME=smart_cs

# ==========================================
# 2. Redis 指令枢纽 (可靠指令链)
# ==========================================
# Redis 服务器地址 (留空则系统自动切换为内存模式，不具备指令持久化能力)
REDIS_HOST=192.168.2.184
# Redis 端口 (默认为 6379)
REDIS_PORT=6379
# Redis 访问密码
REDIS_PASSWORD=123456
# Redis 数据库索引 (默认为 0)
REDIS_DB=0

# ==========================================
# 3. AI 超脑引擎 (Ollama 本地部署)
# ==========================================
# 是否开启 AI 深度语义分析 (true/false)
AI_ENABLED=true
# 调用的模型名称 (推荐 qwen2:1.5b 或 qwen2:7b)
AI_MODEL=qwen2:1.5b
# Ollama 服务接口地址 (方案 B: 指向指挥中心)
AI_URL=http://192.168.2.184:11434/api/chat

# ==========================================
# 4. 服务端设置 (Server Settings)
# ==========================================
# 后端服务绑定地址 (0.0.0.0 表示允许局域网内所有机器连接)
SERVER_HOST=0.0.0.0
# 服务监听端口
SERVER_PORT=8000

# ==========================================
# 5. 安全与认证 (Security & Auth)
# ==========================================
# JWT 加密密钥 (生产环境部署务必修改此项以防伪造 Token)
JWT_SECRET=smart-cs-tactical-link-2024-secure
# 是否开启 WebSocket 握手强制鉴权 (true/false)
AUTH_ENABLED=true
